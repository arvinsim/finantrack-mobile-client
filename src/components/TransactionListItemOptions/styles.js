import { StyleSheet } from 'react-native'

import colors from '../../config/colors'

const styles = StyleSheet.create({
  edit: { backgroundColor: colors.primary },
  delete: { backgroundColor: colors.primary }
})

export default styles